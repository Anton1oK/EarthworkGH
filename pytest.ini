[pytest]
testpaths = tests
# rhino_smoke_test.py must run inside Rhino (it imports RhinoCommon); it is not a
# pure-Python unit test, so keep it out of the offline/CI test run.
addopts = --ignore=tests/rhino_smoke_test.py
